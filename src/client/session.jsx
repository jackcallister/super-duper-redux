import React from 'react';
import ReactDOM from 'react-dom';
import Session from '../shared/components/Session';

document.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(<Session />,document.getElementById('app'));
});
